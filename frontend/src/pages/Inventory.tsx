import Products from "@/pages/Products";

/** Inventory — product & stock management */
export default function Inventory() {
  return (
    <Products
      title="Inventory"
      subtitle="Search items, check stock, set wholesale & retail prices — same clear fields as billing"
    />
  );
}
